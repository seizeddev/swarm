# prettier owns the frontend source (TS/TSX/CSS) + root JSON config only.
# Everything below is either generated, hand-maintained, or owned by another
# formatter (Rust → cargo fmt), so keep prettier out to avoid churn.
node_modules
dist
src-tauri
pnpm-lock.yaml
docs
brand
*.md
# Standalone hand-written design note at the repo root (not frontend source);
# prettier would reformat it on every check. The app's index.html stays managed.
design-prinzipien-ki-apps.html
