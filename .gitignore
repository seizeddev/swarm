# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# test coverage output
coverage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# design scratch (logo concept grids)
design/

# UI/UX design-script transcripts dropped at the repo root (reference notes, not
# source). Root-level only, so the vendored brand/fonts/OFL.txt license stays tracked.
/*.txt

# Claude Code local artifacts (scheduled-task locks, etc.)
.claude/

# Visual brainstorming companion (transient mockups)
.superpowers/
.gstack/
